{
  "name": "laodog-bonus-claim",
  "version": "1.0.0",
  "private": true,
  "description": "Self-service one-time bonus claim portal for dogcoding users.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "start:env": "node --env-file=.env server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs"
  }
}
